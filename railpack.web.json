{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": ["pnpm turbo build --filter=@spiderman/web"]
    }
  },
  "deploy": {
    "startCommand": "node apps/web/.next/standalone/apps/web/server.js"
  }
}
